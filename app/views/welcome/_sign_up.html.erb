<% if user_signed_in? %>
  <%= button_to('Edit', edit_user_registration_path, {method: :get, id: 'banner_btn'}) %>
<% else %>
  <%= button_to('Register', new_user_registration_path, {method: :get, id: 'banner_btn'})  %>
<% end %>
