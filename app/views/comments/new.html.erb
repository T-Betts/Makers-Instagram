<%= form_with(model: [ @picture, @picture.comments.build ], local: true) do |form| %>
<p>
  <%= form.label :body %><br>
  <%= form.text_area :body %>
</p>
<p>
  <%= form.submit :Comment %>
</p>
<% end %>
